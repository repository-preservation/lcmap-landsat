(ns lcmap.aardvark.worker-test
  ""
  (:require [clojure.test :refer :all]
            #_[lcmap.aardvark.worker :as worker]
            [lcmap.aardvark.shared :refer :all]))

#_(deftest worker-tests
  (with-system
    (testing "handle delivery")
    (testing "handle consume")))
